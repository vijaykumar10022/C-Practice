#include<stdio.h>
int main(){
	int num,result;
	num=1975;
	result=sumofdigits(num);
	printf("sum of the digits in %d is %d\n", num,result);
	return 0;
}
int sumofdigits(int n){
	int rem,s;
	if(n!=0){
		rem=n%10;
		s=s+r;
		sumofdigits(num/10);
	}
	else
	return s;
}
